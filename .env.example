# Copy this file to .env in the project root if you want to run Django locally
# The Python settings file uses python-dotenv and will load this file when starting Django from the backend directory.

DATABASE_URL=postgres://nina:nina@localhost:5432/nina
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_SECRET_KEY=dev-only-change-me-nina-organization
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
